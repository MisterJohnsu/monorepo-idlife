import { BaseModel } from '@adonisjs/lucid/orm'

export default class User extends BaseModel {
  // Classe vazia apenas para o Auth não quebrar o boot.
  // Quando quiser usar Auth no futuro, você configura isso direito.
}